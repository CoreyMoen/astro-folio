---
import type { HTMLAttributes } from "astro/types";
import { slotContent } from "@/utils/slots.ts";

type Props = HTMLAttributes<"div"> & {
  /** Skip rendering this component and its children entirely. */
  noRender?: boolean;
};

const { noRender = false, class: className, ...rest } = Astro.props;

const content = await slotContent(Astro.slots);
---

{
  !noRender && content && (
    <div class:list={["eyebrow_wrap", className]} {...rest}>
      <span class="eyebrow_marker" aria-hidden="true" />
      <p class="eyebrow_text text-style-small">
        <slot />
      </p>
    </div>
  )
}

<style is:global>
  @layer components {
    .eyebrow_wrap {
      display: inline-flex;
      align-items: center;
      justify-content: var(--_alignment, start);
      gap: var(--space-2);
    }

    .eyebrow_marker {
      flex: none;
      width: var(--border-width);
      height: 0.75rem;
      background-color: currentcolor;
    }
  }
</style>
